name: CI

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '17 9 * * 1'

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: ${{ matrix.architecture }} / Python ${{ matrix.python-version }} / verify
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.11"
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.12"
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.13"
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.14"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.11"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.12"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.13"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.14"
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # v9.0.0
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
          version: "0.12.3"
      - name: Install just
        uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3  # v4
        with:
          just-version: "1.58.0"
      - name: Set up development environment
        run: just setup
      - name: Run verification suite
        run: just verify
      - name: Retain validated wheel
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: release-wheel-${{ matrix.architecture }}-py${{ matrix.python-version }}
          path: dist/*.whl
          if-no-files-found: error
          retention-days: 7
      - name: Validate hooks
        run: UV_NO_SYNC=1 uv run prek run --all-files

  wheel-smoke:
    needs: ci
    name: ${{ matrix.architecture }} / Python ${{ matrix.python-version }} / wheel smoke
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.11"
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.12"
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.13"
          - architecture: amd64
            runner: ubuntu-24.04
            python-version: "3.14"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.11"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.12"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.13"
          - architecture: arm64
            runner: ubuntu-24.04-arm
            python-version: "3.14"
    steps:
      - name: Check out smoke harness
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # v9.0.0
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
          version: "0.12.3"
      - name: Download validated wheel
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: release-wheel-${{ matrix.architecture }}-py${{ matrix.python-version }}
          path: dist
      - name: Install validated wheel in a fresh environment
        env:
          MATRIX_ARCHITECTURE: ${{ matrix.architecture }}
          MATRIX_PYTHON: ${{ matrix.python-version }}
        run: |
          shopt -s nullglob
          wheels=(dist/*.whl)
          if (( ${#wheels[@]} != 1 )) || [[ ! -f "${wheels[0]:-}" ]]; then
            printf 'architecture=%s python=%s: expected exactly one wheel\n' \
              "${MATRIX_ARCHITECTURE}" "${MATRIX_PYTHON}" >&2
            exit 1
          fi
          uv venv --python "${MATRIX_PYTHON}" .wheel-venv
          uv export --frozen --no-dev --no-emit-project --no-header \
            --output-file .wheel-requirements.txt
          uv pip install --python .wheel-venv/bin/python \
            --requirements .wheel-requirements.txt
          uv pip install --no-deps --python .wheel-venv/bin/python "${wheels[0]}"
      - name: Exercise installed CLI and MCP
        run: |
          .wheel-venv/bin/python - <<'PY'
          from pathlib import Path

          import hmc_mcp

          package_path = Path(hmc_mcp.__file__).resolve()
          environment = Path(".wheel-venv").resolve()
          assert package_path.is_relative_to(environment), package_path
          PY
          .wheel-venv/bin/hmc-mcp --help >/dev/null
          .wheel-venv/bin/hmc-mcp lpars --help >/dev/null
          .wheel-venv/bin/hmc-mcp storage --help >/dev/null
          .wheel-venv/bin/hmc-mcp network --help >/dev/null
          .wheel-venv/bin/hmc-mcp templates --help >/dev/null
          .wheel-venv/bin/hmc-mcp metrics --help >/dev/null
          .wheel-venv/bin/python scripts/smoke_mcp.py

  # ppc64le-release-artifact-template: begin
  # ppc64le:
  #   name: ppc64le / Python 3.12 (QEMU)
  #   runs-on: ubuntu-24.04
  #   timeout-minutes: 30
  #   steps:
  #     - name: Check out repository without credentials
  #       uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
  #       with:
  #         persist-credentials: false
  #     - name: Register the ppc64le emulator
  #       uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8  # v4.2.0
  #       with:
  #         image: docker.io/tonistiigi/binfmt@sha256:400a4873b838d1b89194d982c45e5fb3cda4593fbfd7e08a02e76b03b21166f0
  #         platforms: ppc64le
  #         cache-image: false
  #     - name: Build the ppc64le verification environment
  #       run: >-
  #         docker build --platform linux/ppc64le
  #         --file .github/containers/ppc64le.Dockerfile
  #         --tag hmc-mcp-verify-ppc64le
  #         .github/containers
  #     - name: Run the verification suite under ppc64le emulation
  #       run: >-
  #         docker run --rm --platform linux/ppc64le
  #         --mount type=bind,source="$GITHUB_WORKSPACE",target=/workspace
  #         hmc-mcp-verify-ppc64le
  # ppc64le-release-artifact-template: end

  python-support-drift:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # v9.0.0
        with:
          enable-cache: true
          python-version: "3.11"
          version: "0.12.3"
      - name: Check Python lifecycle policy
        run: uv run --no-project python scripts/check_python_support.py 3.11 3.12 3.13 3.14
